{
  "name": "com.vonage.quickapp.test",
  "version": "0.0.1",
  "description": "Basic Quickapp showing how to use the Vonage Voice API",
  "scripts": {
    "start": "hap server --watch",
    "build": "hap build",
    "release": "hap release",
    "server": "hap server",
    "watch": "hap watch",
    "lint": "eslint --format codeframe --fix --ext .ux,.js src/"
  },
  "devDependencies": {
    "hap-toolkit": "^1.9.5",
    "nexmo-client": "^9.1.3"
  },
  "dependencies": {
    "hash-sum": "^2.0.0",
    "nexmo-client": "^9.1.3",
    "source-map": "^0.7.4"
  },
  "browserslist": [
    "chrome 65"
  ]
}
